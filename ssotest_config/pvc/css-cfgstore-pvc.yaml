apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: css-cfgstore
   annotations:
     trident.netapp.io/unixPermissions: "777"
spec:
   storageClassName: nas-ssd-basic
   accessModes:
     - ReadWriteMany
   resources:
     requests:
       storage: 20Gi
