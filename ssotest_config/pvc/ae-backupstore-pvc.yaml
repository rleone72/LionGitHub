apiVersion: v1
kind: PersistentVolumeClaim
metadata:
   name: ae-backupstore-pvc
   annotations:
     trident.netapp.io/unixPermissions: "777"
spec:
   storageClassName: nas-ssd-basic
   accessModes:
     - ReadWriteMany
   resources:
     requests:
       storage: 100Gi